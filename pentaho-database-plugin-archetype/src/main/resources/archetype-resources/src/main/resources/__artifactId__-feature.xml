#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">

  <feature name="${artifactId}-feature" version="${version}">
    <bundle>mvn:${groupId}/${artifactId}/${version}</bundle>
  </feature>

</features>
